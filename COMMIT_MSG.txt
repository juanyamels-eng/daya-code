feat(cli): top-tier visual identity — welcome hero, branded status, live tool, error card, daya theme

- Welcome hero with block-letter DAYA logo, tagline, and shortcuts
- Fixed brand accent (DAYA_BRAND) surviving every theme + new "daya" theme
- Wordmark "✻ DAYA" on every assistant turn; segmented build/plan pills
- Full-width status bar with colored ctx meter (green→amber→red) and cost/token stats
- Live tool indicator (runningTool); run completion band with mode/dur/cost
- Quantified diffs (+N −M) per file; sections as hairline bands
- Friendly startup error card when provider/config fails (missing API key, etc.)
- Context near-limit warning banner at ≥75%
- Breadcrumb header: project folder + model on the right
- Mode-aware busy label: "planning" in plan, "thinking" in build
- Removed redundant tool prefixes (real rows already carry $/⌕/✗ icons)
- Cleaner tool error coloring via metaColor