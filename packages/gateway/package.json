{
  "name": "@daya-code/gateway",
  "version": "0.1.0",
  "description": "OpenAI-compatible model gateway for DAYA — one endpoint, your keys, per-user quota",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "daya-gateway": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/juanyamels-eng/daya-code",
    "directory": "packages/gateway"
  },
  "keywords": [
    "gateway",
    "proxy",
    "openai-compatible",
    "ai",
    "daya",
    "llm"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@daya-code/core": "^0.5.0",
    "stripe": "^22.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
